# Copy this file to .env and adjust to your user ID if needed.
# Docker Compose reads .env automatically — this ensures the container
# runs as your user so volume-mounted files are owned correctly on Linux.
#
# To get your UID/GID: run `id -u` and `id -g`
UID=1000
GID=1000
