# Quickstart

The simplest possible estampo config: one STL part sliced with OrcaSlicer.

## Run

```sh
estampo run examples/quickstart/estampo.toml
```

## What it demonstrates

- Minimal `estampo.toml` structure
- OrcaSlicer engine with a printer + process profile
- Default pipeline stages: load → arrange → plate → slice
