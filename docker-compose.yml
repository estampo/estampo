services:
  estampo:
    build:
      context: .
      args:
        ORCA_VERSION: "2.3.1"
    image: estampo:orca-2.3.1
    platform: linux/amd64
    volumes:
      - .:/project
    working_dir: /project
    user: "${UID:-1000}:${GID:-1000}"
