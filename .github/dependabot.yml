version: 2
updates:
  - package-ecosystem: "github-actions"
    # "/" covers .github/workflows/; "/action" covers the shipped composite action
    directories:
      - "/"
      - "/action"
    schedule:
      interval: "monthly"
